<icecast>
    <location>RadioDan Studio</location>
    <admin>admin@radiodan.local</admin>

    <limits>
        <clients>10</clients>
        <sources>2</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <burst-size>65535</burst-size>
    </limits>

    <authentication>
        <!-- Source password: used by Liquidsoap to connect -->
        <source-password>__SOURCE_PASSWORD__</source-password>
        <!-- Relay password (not used, but required) -->
        <relay-password>__SOURCE_PASSWORD__</relay-password>
        <!-- Admin password: for web admin interface -->
        <admin-user>admin</admin-user>
        <admin-password>__ADMIN_PASSWORD__</admin-password>
    </authentication>

    <hostname>radiodan.local</hostname>

    <listen-socket>
        <port>8000</port>
        <bind-address>0.0.0.0</bind-address>
    </listen-socket>

    <mount type="normal">
        <mount-name>/stream</mount-name>
        <public>0</public>
        <stream-name>__STATION_NAME__</stream-name>
        <stream-description>Ambient AI work companion</stream-description>
        <genre>Ambient/Electronic</genre>
    </mount>

    <fileserve>1</fileserve>

    <paths>
        <basedir>/usr/share/icecast</basedir>
        <logdir>/var/log/icecast</logdir>
        <webroot>/usr/share/icecast/web</webroot>
        <adminroot>/usr/share/icecast/admin</adminroot>
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>3</loglevel>
        <logsize>10000</logsize>
    </logging>

    <security>
        <chroot>0</chroot>
        <changeowner>
            <user>icecast</user>
            <group>icecast</group>
        </changeowner>
    </security>
</icecast>
