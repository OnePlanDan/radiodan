# RadioDan - Python Dependencies

# Telegram bot framework (async)
python-telegram-bot[job-queue]>=21.0

# Environment and configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Async HTTP client (for TTS/STT calls)
aiohttp>=3.9.0

# Async SQLite (config store)
aiosqlite>=0.20.0

# Web GUI (Jinja2 templates for aiohttp)
aiohttp-jinja2>=1.6
jinja2>=3.1.0

# ID3 tag reading for music library scanning
mutagen>=1.47.0

# Type hints support
typing-extensions>=4.9.0
